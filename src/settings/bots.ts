export const MAX_DATABASE_BLOCKS = 10;

export const DROP_ZONE_IDS = {
	availableTemplate: "available-template",
	availableDatabase: "available-database",
	activeTemplate: "active-template",
	activeDatabase: "active-database",
} as const;
